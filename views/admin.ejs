<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - Whisp</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="admin-page">
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
      <span class="navbar-brand mb-0 h1">Whisp Admin Dashboard</span>
      <div class="d-flex align-items-center text-white gap-3">
        <span class="me-2">Welcome, <%= adminUsername %></span>
        <div class="d-flex align-items-center gap-2">
          <i class="bi bi-sun-fill theme-toggle-icon" id="lightIcon"></i>
          <label class="theme-toggle mb-0">
            <input type="checkbox" id="themeToggle">
            <span class="theme-toggle-slider"></span>
          </label>
          <i class="bi bi-moon-fill theme-toggle-icon" id="darkIcon"></i>
        </div>
        <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <!-- Stats -->
    <div class="row mb-4">
      <div class="col-md-6 mb-3">
        <div class="card border-0 shadow-sm">
          <div class="card-body">
            <div class="d-flex align-items-center">
              <div class="flex-grow-1">
                <h6 class="text-muted mb-1">Total Users</h6>
                <h2 class="mb-0"><%= totalUsers %></h2>
              </div>
              <div class="stat-icon bg-primary">
                <i class="bi bi-people-fill"></i>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-6 mb-3">
        <div class="card border-0 shadow-sm">
          <div class="card-body">
            <div class="d-flex align-items-center">
              <div class="flex-grow-1">
                <h6 class="text-muted mb-1">Pending Approvals</h6>
                <h2 class="mb-0"><%= pendingUsers %></h2>
              </div>
              <div class="stat-icon bg-warning">
                <i class="bi bi-clock-fill"></i>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Users table -->
    <div class="card border-0 shadow-sm mb-4">
      <div class="card-header bg-white">
        <h5 class="mb-0">User Management</h5>
      </div>
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead class="table-light">
              <tr>
                <th>Username</th>
                <th>Status</th>
                <th>Created At</th>
                <th>Last Seen</th>
                <th class="text-end">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (users.length === 0) { %>
                <tr>
                  <td colspan="5" class="text-center text-muted py-4">No users found</td>
                </tr>
              <% } else { %>
                <% users.forEach(user => { %>
                  <tr>
                    <td>
                      <div class="d-flex align-items-center">
                        <div class="avatar-sm me-2">
                          <%= user.username.charAt(0).toUpperCase() %>
                        </div>
                        <span><%= user.username %></span>
                      </div>
                    </td>
                    <td>
                      <% if (user.status === 'pending') { %>
                        <span class="badge bg-warning">Pending</span>
                      <% } else if (user.status === 'approved') { %>
                        <span class="badge bg-success">Approved</span>
                      <% } else { %>
                        <span class="badge bg-danger">Rejected</span>
                      <% } %>
                    </td>
                    <td><%= new Date(user.createdAt).toLocaleDateString() %></td>
                    <td><%= new Date(user.lastSeen).toLocaleDateString() %></td>
                    <td>
                      <div class="d-flex gap-1 justify-content-end">
                        <% if (user.status === 'pending') { %>
                          <form action="/admin/approve/<%= user._id %>" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-success" title="Approve">
                              <i class="bi bi-check-lg"></i>
                            </button>
                          </form>
                        <% } %>

                        <button type="button" class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#resetPasswordModal<%= user._id %>" title="Reset Password">
                          <i class="bi bi-key-fill"></i>
                        </button>

                        <form action="/admin/delete/<%= user._id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this user?')">
                          <button type="submit" class="btn btn-sm btn-danger" title="Delete">
                            <i class="bi bi-trash-fill"></i>
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>

                  <!-- Reset Password Modal -->
                  <div class="modal fade" id="resetPasswordModal<%= user._id %>" tabindex="-1">
                    <div class="modal-dialog">
                      <div class="modal-content">
                        <div class="modal-header">
                          <h5 class="modal-title">Reset Password for <%= user.username %></h5>
                          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <form action="/admin/reset-password/<%= user._id %>" method="POST">
                          <div class="modal-body">
                            <div class="mb-3">
                              <label for="newPassword<%= user._id %>" class="form-label">New Password</label>
                              <input type="password" class="form-control" id="newPassword<%= user._id %>" name="newPassword" required minlength="6">
                              <div class="form-text">At least 6 characters</div>
                            </div>
                          </div>
                          <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-primary">Reset Password</button>
                          </div>
                        </form>
                      </div>
                    </div>
                  </div>
                <% }); %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Deleted Messages table -->
    <div class="card border-0 shadow-sm">
      <div class="card-header bg-white">
        <h5 class="mb-0">Deleted Messages</h5>
        <small class="text-muted">Showing last 50 deleted messages</small>
      </div>
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover mb-0">
            <thead class="table-light">
              <tr>
                <th>From</th>
                <th>To</th>
                <th>Message</th>
                <th>Deleted By</th>
                <th>Deleted At</th>
                <th class="text-end">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (deletedMessages.length === 0) { %>
                <tr>
                  <td colspan="6" class="text-center text-muted py-4">No deleted messages</td>
                </tr>
              <% } else { %>
                <% deletedMessages.forEach(msg => { %>
                  <tr>
                    <td>
                      <div class="d-flex align-items-center">
                        <div class="avatar-sm me-2">
                          <%= msg.from.username.charAt(0).toUpperCase() %>
                        </div>
                        <span><%= msg.from.username %></span>
                      </div>
                    </td>
                    <td>
                      <div class="d-flex align-items-center">
                        <div class="avatar-sm me-2">
                          <%= msg.to.username.charAt(0).toUpperCase() %>
                        </div>
                        <span><%= msg.to.username %></span>
                      </div>
                    </td>
                    <td>
                      <div class="text-truncate" style="max-width: 300px;" title="<%= msg.message %>">
                        <%= msg.message.length > 50 ? msg.message.substring(0, 50) + '...' : msg.message %>
                      </div>
                    </td>
                    <td>
                      <% if (msg.deletedBy) { %>
                        <%= msg.deletedBy.username %>
                      <% } else { %>
                        <span class="text-muted">Unknown</span>
                      <% } %>
                    </td>
                    <td>
                      <% if (msg.deletedAt) { %>
                        <%= new Date(msg.deletedAt).toLocaleString() %>
                      <% } else { %>
                        <span class="text-muted">Unknown</span>
                      <% } %>
                    </td>
                    <td>
                      <div class="d-flex gap-1 justify-content-end">
                        <button type="button" class="btn btn-sm btn-info" data-bs-toggle="modal" data-bs-target="#viewMessageModal<%= msg._id %>" title="View Full Message">
                          <i class="bi bi-eye-fill"></i>
                        </button>
                        <form action="/admin/restore-message/<%= msg._id %>" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to restore this message?')">
                          <button type="submit" class="btn btn-sm btn-success" title="Restore">
                            <i class="bi bi-arrow-counterclockwise"></i>
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>

                  <!-- View Message Modal -->
                  <div class="modal fade" id="viewMessageModal<%= msg._id %>" tabindex="-1">
                    <div class="modal-dialog">
                      <div class="modal-content">
                        <div class="modal-header">
                          <h5 class="modal-title">Message Details</h5>
                          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body">
                          <div class="mb-3">
                            <strong>From:</strong> <%= msg.from.username %>
                          </div>
                          <div class="mb-3">
                            <strong>To:</strong> <%= msg.to.username %>
                          </div>
                          <div class="mb-3">
                            <strong>Sent:</strong> <%= new Date(msg.timestamp).toLocaleString() %>
                          </div>
                          <div class="mb-3">
                            <strong>Deleted By:</strong>
                            <% if (msg.deletedBy) { %>
                              <%= msg.deletedBy.username %>
                            <% } else { %>
                              Unknown
                            <% } %>
                          </div>
                          <div class="mb-3">
                            <strong>Deleted At:</strong>
                            <% if (msg.deletedAt) { %>
                              <%= new Date(msg.deletedAt).toLocaleString() %>
                            <% } else { %>
                              Unknown
                            <% } %>
                          </div>
                          <div class="mb-3">
                            <strong>Message:</strong>
                            <div class="p-3 bg-light rounded mt-2" style="white-space: pre-wrap; word-wrap: break-word;">
                              <%= msg.message %>
                            </div>
                          </div>
                        </div>
                        <div class="modal-footer">
                          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        </div>
                      </div>
                    </div>
                  </div>
                <% }); %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Theme management - Load from database
    const loadTheme = async () => {
      try {
        const response = await fetch('/admin/theme');
        const data = await response.json();
        const theme = data.theme || 'light';

        if (theme === 'dark') {
          document.body.classList.add('dark-mode');
        } else {
          document.body.classList.remove('dark-mode');
        }

        const themeToggle = document.getElementById('themeToggle');
        if (themeToggle) {
          themeToggle.checked = theme === 'dark';
        }
      } catch (error) {
        console.error('Error loading theme:', error);
      }
    };

    // Initialize theme on page load
    loadTheme();

    // Theme toggle (admin only)
    const themeToggle = document.getElementById('themeToggle');
    if (themeToggle) {
      themeToggle.addEventListener('change', async () => {
        const theme = themeToggle.checked ? 'dark' : 'light';

        // Apply theme immediately
        if (theme === 'dark') {
          document.body.classList.add('dark-mode');
        } else {
          document.body.classList.remove('dark-mode');
        }

        // Save to database
        try {
          await fetch('/admin/theme', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({ theme })
          });
        } catch (error) {
          console.error('Error saving theme:', error);
        }
      });
    }
  </script>
</body>
</html>
